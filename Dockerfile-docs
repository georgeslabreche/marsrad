# Dockerfile for generating marsrad package PDF documentation
#
# Lightweight container with R and minimal TeX for building the package reference manual.
#
# Usage (from repository root):
#   # On Apple Silicon Macs:
#   docker build --platform linux/amd64 -t marsrad-manual -f Dockerfile-docs .
#   docker run --rm --platform linux/amd64 -v $(pwd):/output marsrad-manual
#
#   # On Intel Macs/Linux:
#   docker build -t marsrad-manual -f Dockerfile-docs .
#   docker run --rm -v $(pwd):/output marsrad-manual
#
# The generated PDF will be written to marsrad-manual.pdf

FROM --platform=linux/amd64 r-base:latest

# Install minimal TeX packages needed for R CMD Rd2pdf
RUN apt-get update && apt-get install -y \
    texlive-latex-base \
    texlive-fonts-recommended \
    texlive-latex-extra \
    texlive-fonts-extra \
    && rm -rf /var/lib/apt/lists/*

# Set working directory
WORKDIR /pkg

# Copy package files
COPY . /pkg/

# Generate the PDF documentation from man/ directory only
CMD R CMD Rd2pdf --no-preview --title="marsrad: Mars Solar Radiation Package Reference Manual" /pkg/man --output=/output/marsrad-manual.pdf && \
    echo "PDF generated successfully at /output/marsrad-manual.pdf"
